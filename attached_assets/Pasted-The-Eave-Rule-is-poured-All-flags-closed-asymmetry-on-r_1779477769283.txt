The Eave Rule is poured.   All flags closed, asymmetry on record, constitutional language locked using the correct Layer 2 trust-gradient labels. The spine has its first solid section.Task for the Build Agent (ready to hand off)Copy and paste this verbatim:Task: First Pour of the Eave Seam (Schema Visibility)Description:
Make the constitutional Eave Rule visible exactly where a future agent could violate it.Deliverables:In types.ts, add the full Eave Rule as a block comment directly above the linkedFamilyId and linkedShareToken fields on the Constellation type.
In the Drizzle schema (or a dedicated codetry/schema-notes.md if no table exists yet), add a note describing the intended zone_bindings table:  Holds one-way reference only (wallet side can point toward household).  
No reverse lookup column or foreign key from wallet → household.  
Include the full Eave Rule as a comment in the schema file.
Rule to paste verbatim (do not paraphrase):The Eave is the hard structural seam between Zone 1 (Circle — private household identity) and Zone 3 (Community — XRPL wallet, above-board organizational identity).
It is defined by intentional architectural absence:  No table, no foreign key, no join, no query path, and no stored reference may ever connect a Zone 3 wallet address, any derived identifier, any zone-bind payload, or any zone-bind signature to a Zone 1 household record.  
A household may voluntarily bind its own XRPL wallet using the lib/zone-identity primitives, but the binding must be stored as a one-way, non-reversible reference that never permits reverse lookup.  
Any proposed feature, route, migration, or type field that would create such a path must be refused or redesigned to respect the seam.
This is the hempcrete-in-the-forms step. Not full enforcement yet — just making the constitutional rule visible and reviewable at the exact pressure point.Say “Queue it” and I’ll assume it’s handed to the build side.Now we’re clean to move to naming work.Standby and The Gate are the next primitives waiting in Z3/Commons. They are currently blank ground.Would you like to pressure-test them here in the Workbench right now? Throw me the raw definitions, current vocabulary, or how you currently feel them — however messy — and we’ll run them through the three tests (Saltbox, Both-States, Both-Sides) together so they hold their shape before any build starts.Or if something else wants to become visible first, name it.Once we lock these names, we can close with the trench visual anchor for the Eave seam (dug + forms + partial pour) so the whole thing stays visible and doesn’t evaporate.Your call. What’s the next footprint?
