/**
 * Re-export from the shared workspace package so existing imports
 * inside codetry-ship continue to work without any path changes.
 */
export { ZoneTag, ZoneDot, ZONE_COLORS } from "@workspace/zone-tag";
export type { ZoneTagProps, ZoneDotProps, ZoneColorEntry } from "@workspace/zone-tag";
